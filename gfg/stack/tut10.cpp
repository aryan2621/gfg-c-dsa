// Infix to Prefix Conversion

/*
Operartor      Associatiity

^             right to left    ^
* /            left to right   |   precedence
+ -           left to right    |

*/

// i-> infix = x+y * z;
// o-> prefix = + x*y z;

// i-> infix = (x+y) * z;
// o-> prefix = *+ xy z;

// i-> infix = x^y ^ z;
// o-> prefix = ^x ^ y z;

// i-> infix = (x+y) * (z+w);
// o-> prefix = * +xy + zw;


