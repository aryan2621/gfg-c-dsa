// Traversaal on the tree;

// Printing all the elments in the tree present at that time,

// Tree traversal can be broadly classified as the two types
// -> Breadth First
// ( Print the root element and then print the child nodes from left to right)
// writing a page or reading a book

// -> Depth First
// we chose one side finish it completely then move to the other side;
// First we move to root then left side and process them then move to the right side
// process them(similar way we do in the proces of recursion);

//          -> Transverse root
// recursive -> Transverse left side
//           -> Transverse right side;

// inorder -> left root right
// preorder ->root left right
// postorder-> left right root;

// left always before first


